antes de usar este script, se necesita tener instalado python 3, a demas de usar estos comandos en el CMD o terminal

pip install tkinter
pip install pandas
pip install python-docx
pip install matplotlib
pip install openpyxl