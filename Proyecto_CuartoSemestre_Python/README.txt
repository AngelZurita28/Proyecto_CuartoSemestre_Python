antes de usar este script, se necesita tener instalado python 3, a demas de usar estos comandos en la terminal para installar los paquetes necesarios

pip install tkinter
pip install pandas
pip install python-docx
pip install matplotlib
pip install openpyxl
pip install request
pip install pywin32
pip install pdfdocument
pip install requests